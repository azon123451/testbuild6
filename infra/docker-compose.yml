version: "3.9"

services:
  bot:
    build:
      context: ..
      dockerfile: infra/bot.Dockerfile
    env_file:
      - ../.env
    volumes:
      - ../bot:/app/bot
      - ../shared:/app/shared
      - ../data:/app/data
    restart: unless-stopped

